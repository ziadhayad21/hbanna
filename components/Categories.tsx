import CategoryGrid from "@/components/CategoryGrid";

export default function Categories() {
  return (
    <section id="categories" className="section-pad">
      <CategoryGrid showViewAll />
    </section>
  );
}
